---
title: 利用相关服务提权(烂土豆系列)
date: 2025-03-04 02:01:17
updated: 2026-05-30 06:41:17
tags:
  - 内网
  - 权限提升
---
<details open>
<summary>Potato提权小结</summary>

原理简单来说就是

（1）诱使 “SYSTEM” 账户通过 NTLM 向控制的 TCP 节点进行身份验证

（2）以本地协商 “NT AUTHORITY \\ SYSTEM” 账户的安全令牌进行 NTLM Relay

（3）模拟刚刚协商的令牌，达到提权的目的

</details>

利用 Potato 提权的前提是**拥有 SeImpersonatePrivilege** 或 SeAssignPrimaryTokenPrivilege(SYSTEM) 权限

以下用户拥有 **SeImpersonatePrivilege** 权限:

-   本地管理员账户(不包括管理员组普通账户) administrator
-   本地服务账户 nt /service

Windows 服务的登陆账户

-   Local System(NT AUTHORITY\\SySTEM)
-   Network Service(NT AUTHORITY\\Network Service)
-   Local Service(NT AUTHORITY\\Local Service)

也就是说该提权是

-   ​Administrator---->SYSTEM
-   Service---->SYSTEM

```shell
whoami /priv
whoami /priv | findstr "SeImpersonatePrivilege"
```

# 使用场景

◾ 在具有 webshell 下，用户权限是 IIS 或者 apache

◾ 通过 SQLi 执行 xp\_cmdshell

◾ 通过 mssql 口令执行 xp\_cmdshell，执行 mssql clr

在以上场景中，如果 **web 服务或 mssql 是服务账号（nt\\service）**启动的，那么我们继承的就是服务账号的低权限，此时利用该提权手法可直接获取 SYSTEM 权限，即 servie>system。

**通过各种方法在本地 NTLM 中继获取 SYSTEM 令牌，再通过模拟令牌执行命令，通过以上方法提权统称为 potato。**

# 常用

## SweetPotato

[GitHub - uknowsec/SweetPotato: Modifying SweetPotato to support load shellcode and webshell](https://github.com/uknowsec/SweetPotato)

Win7-Win10/Win2008-2019

```python
SweetPotato.exe -a "whoami"
execute-assembly /root/SweetPotato_new.exe -a "C:\inetpub\wwwroot\artifact.exe"
execute-assembly /root/SweetPotato.exe -a "C:\inetpub\wwwroot\artifact.exe"
```

## BadPotato

[GitHub - BeichenDream/BadPotato: Windows 权限提升 BadPotato](https://github.com/BeichenDream/BadPotato)

Windows 2012-2019、Windows 8-10

```python
BadPotatoNet2.exe "whoami"
execute-assembly /root/BadPotatoNet2.exe "C:\inetpub\wwwroot\artifact.exe"
```

# 历史土豆

## HOT Potato

[GitHub - foxglovesec/Potato](https://github.com/foxglovesec/Potato)

需要等待 windows update

DBNS 欺骗，WPAD 和 Windows update 服务

原理：

（1）通过 HOST-DNS 使 UDP 端口耗尽——NBNS

（2）通过 fake WPAD prxy Server（劫持 http）

（3）HTTP>SMB NTML Relay

## Rotten Potato

[GitHub - foxglovesec/RottenPotato: RottenPotato local privilege escalation from service account to SYSTEM](https://github.com/foxglovesec/RottenPotato)

它是通过 DCOM call 来使服务向攻击者监听的端口发起连接并进行 NTLM 认证,需要 SelmpersonatePrivilege 权限

## Juicy Potato

Rotten Potato 的加强版

[GitHub - ohpe/juicy-potato: A sugared version of RottenPotatoNG, with a bit of juice, i.e. another Local Privilege Escalation tool, from a Windows Service Accounts to NT AUTHORITY\SYSTEM.](https://github.com/ohpe/juicy-potato)

-   ​本地支持 RPC 或远程服务器支持 RPC 并能成功登陆
-   用户支持 Selmpersonate 或 SeAssignPrimaryToken 权限
-   开启 DCOM
-   可用的 COM 对象

## Ghost Potato

[GitHub - Ridter/GhostPotato: Just pick out the code we need.](https://github.com/Ridter/GhostPotato)

利用 CVE-2019-1384(Ghost Potato) Bypass MS-08068

## Pipe Potato

攻击者通过 pipeserver.exe 注册一个名为 pipexpipespoolss 的恶意的命名管道等待高权限用户来连接以模拟高权限用户权限，然后通过 spoolssClient.exe 迫使system 用户来访问攻击者构建的恶意命名管道,从而模拟 system 用户运行任意应用程序

原理：

（1）调用 CreateN amedPipe()创建一个命名管道

（2）调用 ConnectNamedPipe() 接受该命名请求连接

（3）迫使高权限进程连接该命名管道并写入数据

（4）调用 ImpersonateNamedPipeClient()派生 一个高权限进程的客户端

衍生版本：

PrintSpoofer

[GitHub - itm4n/PrintSpoofer: Abusing impersonation privileges through the “Printer Bug”](https://github.com/itm4n/PrintSpoofer)

badpotapo

.Net 版本的 pipePotato

https://github.com/BeichenDream/BadPotato

利用 spoolsv.exe 进程的 RPC 服务器强制 Windows 主机向其他计算机进行身份证验证

需要 SelmpersonatePrivilege 、SeAssignPrimaryToken 权限

## Sweet Potato

juicy potato 的重写

[GitHub - CCob/SweetPotato: Local Service to SYSTEM privilege escalation from Windows 7 to Windows 10 / Server 2019](https://github.com/CCob/SweetPotato)

COM/WinRM/Spoolsv 的集合版

也就是 Juicy/PrintSpoofer

从 Windows 7 到 windows10/windows server2019 的本地服务到 system 特权升级